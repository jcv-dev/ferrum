# Ferrum Configuration
# Copy this file to .env and adjust as needed

# Server configuration
HOST=0.0.0.0
PORT=8080

# Path to your music library
MUSIC_FOLDER=./music

# Path to store user data (will be created if it doesn't exist)
USERS_FILE=./data/users.json

# JWT secret key for authentication (min 32 characters recommended)
# IMPORTANT: Change this in production!
JWT_SECRET=your-super-secret-key-change-in-production

# JWT token expiry in days
JWT_EXPIRY_DAYS=7

# Logging configuration
# Levels: trace, debug, info, warn, error
LOG_LEVEL=info

# Log format: pretty (human-readable) or json (structured)
LOG_FORMAT=pretty

# CORS allowed origins (comma-separated, or * for all)
# Example: http://localhost:3000,https://myapp.com
CORS_ORIGINS=*
